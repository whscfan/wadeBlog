{% extends "blog/blogBase.html" %}
{% block navBar %}
    {% include "blog/widgets/navBar.html" %}
{% endblock %}
{% block mainContent %}

    <div class="container">
        <div class="row">
            <div class="col-lg-12 col-md-12">
                <div class="lt-jumbotrons">
                    <div class="jumbotron jumbotron-1">
                        <h1>我要说的话：</h1>

                        <p>{{ word|safe }}</p>

                    </div>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-sm-12">
                <div class="lt-tabs">
                    <div class="tabbable tabs-left">
                        <ul class="nav nav-tabs">
                            {% for about in about %}
                                {% if forloop.counter == 1 %}
                                    <li class="active"><a href="#{{ forloop.counter }}" data-toggle="tab" aria-expanded="false"><i
                                    class="fa fa-facebook"></i> {{ about }}</a></li>
                                {% else %}
                                    <li ><a href="#{{ forloop.counter }}" data-toggle="tab" aria-expanded="false"><i
                                    class="fa fa-facebook"></i> {{ about }}</a></li>
                                {% endif %}
                            {% endfor %}
                        </ul>
                        <div class="tab-content" style="background-color: #212122" >
                            {% for key,text in about.items %}
                                {% if forloop.counter == 1 %}
                                <div class="tab-pane active" id="{{ forloop.counter }}">
                                     <p style="color: #FFF;">{{ text|safe }}</p>
                                </div>
                                {% else %}
                                    <div class="tab-pane "  id="{{ forloop.counter }}">
                                    <p style="color: #FFF;">{{ text|safe }}</p>
                                </div>
                                {% endif %}
                            {% endfor %}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
{% block footer %}
    {% include "blog/widgets/footer.html" %}
{% endblock %}